# はじまりの島 — 仕様

## 0. ドキュメント情報
- 対象ゲーム: 244-hajimari-no-shima
- 更新日: 2026-09-20（操作UI再設計）
- ステータス: 公開
- 参照ファイル: index.html / style.css / game.js / three.min.js

## 1. ゲーム概要
- ジャンル: 短編3D探索RPG
- 一言説明: 3Dフィールドで３つの祠を巡り、北の古城の竜を倒す。
- 想定プレイ時間: 10〜20分
- 対象端末: iPhone Safari / GitHub Pages

## 2. 対象環境
- 配信先: GitHub Pages
- 最優先端末: iPhone Safari
- 対応画面幅: 320px〜430px
- 実装方式: 静的 HTML / CSS / JS。Three.js はローカル同梱

## 3. 操作 / 設定UI（必須）
操作盤は**モード切替型**。探索・戦闘・会話で下段25%の中身を丸ごと入れ替える（3枚のパネルを同じグリッドセルに重ね、`visibility` だけで切り替え。`display:none` は使わない＝非表示ボタンも48px以上を保ちハーネスに通すため）。

| モード | 操作盤の中身 | 切替のきっかけ |
|---|---|---|
| `field`（探索） | 十字キー ／ ◀▶視点 ／ 音・Ⅱ ／ 調べる・傷薬 | 開始・会話終了・戦闘離脱 |
| `battle`（戦闘） | 斬る・焔・傷薬・構える・退く・Ⅱ の6枠（3列×2段） | 敵と接触 |
| `talk`（会話） | 全幅の「つづける」大ボタン ＋ 音・Ⅱ | `say()` |

- 移動: 十字キー（Pointer Events・`setPointerCapture`）／ WASD・矢印 ／ ゲームパッド左スティック
- **視点回転（3系統）**: 画面の左右ドラッグ（`#screen-wrap`、左へ引くと視点が左へパン＝ボタンと同方向）／ 操作盤の ◀▶ 視点ボタン（押しっぱなしで回り続ける）／ Q・R キー・ゲームパッド LB RB
- **画面タップ**: 移動12px未満かつ400ms未満の軽いタップで「調べる」＝会話送り。ボタン上のタップは拾わない。押している間に状態が変わったタップは無効（開始タップが最初の会話まで閉じる事故の対策）
- 「調べる」ボタン: 近くに対象があるとラベルが「調べる」になり脈動する。無いときは「見まわす」
- 傷薬: 残数をボタンに常時表示。0個で減光
- ポーズ: 操作盤のⅡ（探索・戦闘・会話の全モードに配置）、Pキー、Escape、タブ非表示
- ミュート: 操作盤の「音 入／切」。絵文字は環境で描画が壊れるため日本語表記。`localStorage` キー `tg.244.mute`
- 冒険セーブ: 既存キー `dq3d-island-v1` を維持（移行なし）
- 画面構成: 上75% `#game-stage` / 下25% `#control-deck`（ハーネス契約）
- HUD: プレイ中はブランドロゴを隠す（ステータス枠と重なるため）。戦闘中はクエストと地図を伏せ、敵名・敵HPは下部の戦闘パネルへ統合する

## 4. コアループ
- 里で回復 → 祠で光を集める → 遭遇戦闘 → 古城の竜
- 戦闘: 斬る / 焔 / 傷薬 / 構える / 退く（操作盤に3列×2段で表示）
- フィールド敵: 潮牙（四足）/ 石童（岩）/ 棘羽（翅）/ 灯核（結晶）/ 甲角（角虫）＋ボス灰翼の竜
- 敗北: 所持貨半減で里へ復帰

## 5. 音声
- 開始タップおよび最初の pointerdown / keydown で AudioContext unlock
- WebAudio のループBGM（`.loop = true`）と効果音
- ミュート時はBGM停止

## 6. 未確定事項
- なし（公開ブロッカーなし）
